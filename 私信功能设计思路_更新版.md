# 💬 三端私信功能 - 集成方案

## 📋 功能概述

在学生、教师、家长三个端的主页添加"💬 消息"入口，点击后打开私信界面。

---

## 🏠 集成到现有系统

### 1. 顶部导航栏添加消息入口

**每个端的主页顶部导航栏：**

```vue
<el-menu>
  <el-menu-item>首页</el-menu-item>
  <el-menu-item>💬 消息 <el-badge :value="unreadCount" v-if="unreadCount > 0"></el-badge></el-menu-item>
  <el-menu-item>设置</el-menu-item>
</el-menu>
```

### 2. 消息入口的视觉效果

```
┌────────────────────────────────────────────┐
│  AI五感作文训练平台          💬 消息(5) 👤  │
└────────────────────────────────────────────┘
```

**说明：**
- 右上角显示"💬 消息"
- 未读消息用红色角标显示数量
- 点击后打开消息界面

---

## 📄 界面设计

### 消息首页（未选中任何对话时）

```
┌─────────────────────────────────────────────────────────┐
│  返回  消息                                [+新建对话]   │
├──────────────────┬────────────────────────────────────┤
│                  │                                    │
│   消息列表       │      空状态                        │
│                  │                                    │
│  ┌──────────┐   │   🌟                               │
│  │ 📩 张老师  │   │  美好的一天                         │
│  │ 今天作文...│   │  从沟通开始                        │
│  │ 19:30  2 │   │                                    │
│  └──────────┘   │                                    │
│                  │                                    │
│  ┌──────────┐   │                                    │
│  │ 📩 家长A  │   │                                    │
│  │ 孩子最近..│   │                                    │
│  │ 昨天  ✓   │   │                                    │
│  └──────────┘   │                                    │
│                  │                                    │
│  ┌──────────┐   │                                    │
│  │ 李老师     │   │                                    │
│  │ 你的作文...│   │                                    │
│  │ 周一   ✓   │   │                                    │
│  └──────────┘   │                                    │
│                  │                                    │
└──────────────────┴────────────────────────────────────┘
```

### 选中对话后

```
┌─────────────────────────────────────────────────────────┐
│  返回  消息 - 张老师                      [+新建对话]     │
├──────────────────┬────────────────────────────────────┤
│                  │                                      │
│   消息列表       │      聊天窗口                        │
│                  │                                      │
│  ┌──────────┐   │  ┌────────────────────────────────┐ │
│  │ 📩 张老师  │   │  │ 👨‍🏫 张老师                      │ │
│  │ 今天作文...│◄──┤  │                                │ │
│  │ 19:30  2  │   │  │    [你好，请问今天...]  19:30  │ │
│  └──────────┘   │  │    ✓✓                          │ │
│                  │  │                                │ │
│  📩 家长A        │  │    [我的作文写完了]  19:35      │ │
│  孩子最近..      │  │    ✓                           │ │
│  昨天  ✓         │  │                                │ │
│                  │  ├────────────────────────────────┤ │
│  📩 李老师       │  │ [输入消息...]        [发送]      │ │
│  你的作文...     │  └────────────────────────────────┘ │
│  周一   ✓        │                                      │
└──────────────────┴────────────────────────────────────┘
```

---

## 🔄 交互流程

### 场景1：学生查看消息

```
1. 学生登录 → 进入学生首页
2. 点击顶部导航栏的"💬 消息"
3. 打开消息页面（左侧列表，右侧空状态）
4. 点击"张老师"对话
5. 右侧显示与张老师的聊天记录
6. 可以输入消息并发送
```

### 场景2：接收新消息

```
1. 用户在首页做其他事情
2. 收到新消息
3. 顶部导航栏显示：💬 消息(3)
4. 点击进入消息界面
5. 未读对话显示在顶部（带红色角标）
6. 点击查看并回复
```

---

## 🎨 UI组件设计

### 1. 导航栏消息入口

**每个端（学生、教师、家长）的主页导航栏：**

```vue
<!-- NavBar.vue 或各端主页 -->
<template>
  <div class="navbar">
    <!-- 左侧 -->
    <div class="navbar-left">
      <span class="logo">AI五感作文训练平台</span>
    </div>
    
    <!-- 右侧 -->
    <div class="navbar-right">
      <!-- 消息入口 -->
      <el-button text @click="openMessages">
        💬 消息
        <el-badge :value="unreadCount" v-if="unreadCount > 0" />
      </el-button>
      
      <!-- 用户信息 -->
      <el-button text>👤 退出</el-button>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { messageAPI } from '@/api/message'

const unreadCount = ref(0)

// 获取未读消息数
const loadUnreadCount = async () => {
  try {
    const count = await messageAPI.getUnreadCount()
    unreadCount.value = count
  } catch (error) {
    console.error('加载未读数失败', error)
  }
}

// 打开消息界面
const openMessages = () => {
  router.push('/messages')
}

onMounted(() => {
  loadUnreadCount()
  // 每30秒刷新一次未读数
  setInterval(loadUnreadCount, 30000)
})
</script>
```

### 2. 消息页面路由

**路由配置：**

```javascript
// router/index.js
{
  path: '/messages',
  name: 'Messages',
  component: () => import('@/pages/Common/Messages.vue')
}
```

### 3. 消息页面组件结构

```vue
<!-- pages/Common/Messages.vue -->
<template>
  <div class="messages-page">
    <!-- 顶部栏 -->
    <div class="messages-header">
      <el-button icon="ArrowLeft" @click="goBack">返回</el-button>
      <span class="header-title">消息</span>
      <el-button icon="Plus" @click="showNewChatDialog">新建对话</el-button>
    </div>
    
    <div class="messages-content">
      <!-- 左侧：消息列表 -->
      <div class="messages-list">
        <MessageList 
          :conversations="conversations"
          :selectedConversation="selectedConversation"
          @select="onSelectConversation"
        />
      </div>
      
      <!-- 右侧：聊天窗口 -->
      <div class="messages-chat">
        <ChatWindow 
          v-if="selectedConversation"
          :partner="selectedConversation"
          @send="onSendMessage"
        />
        <EmptyState v-else />
      </div>
    </div>
    
    <!-- 新建对话对话框 -->
    <el-dialog v-model="showNewChat" title="新建对话" width="400px">
      <!-- 选择收信人 -->
      <!-- 输入消息 -->
    </el-dialog>
  </div>
</template>
```

---

## 📊 数据结构

### 前端对话列表数据

```javascript
conversations: [
  {
    partner: {
      id: 123,
      username: '张老师',
      role: 'teacher',
      avatar: '👨‍🏫'
    },
    lastMessage: {
      content: '今天作文怎么写...',
      createdAt: '2024-01-15 19:30:00',
      isRead: false,
      senderId: 123
    },
    unreadCount: 2
  },
  {
    partner: {
      id: 456,
      username: '妈妈',
      role: 'parent',
      avatar: '👨‍👩‍👧'
    },
    lastMessage: {
      content: '孩子最近学习怎么样...',
      createdAt: '2024-01-14 16:00:00',
      isRead: true,
      senderId: 456
    },
    unreadCount: 0
  }
]
```

---

## 🔧 后端API设计

### API端点

```java
@RestController
@RequestMapping("/api/message")
public class MessageController {
    
    // GET /api/message/conversations
    // 获取所有对话列表
    List<ConversationDTO> getConversations();
    
    // GET /api/message/conversation/:partnerId
    // 获取与某人的对话记录
    List<MessageDTO> getConversation(@PathVariable Long partnerId);
    
    // POST /api/message/send
    // 发送消息
    // Body: { receiverId: 123, content: "你好" }
    MessageDTO sendMessage(@RequestBody SendMessageRequest request);
    
    // PUT /api/message/read/:messageId
    // 标记消息已读
    void markAsRead(@PathVariable Long messageId);
    
    // GET /api/message/unread-count
    // 获取未读消息数
    long getUnreadCount();
}
```

### DTO设计

```java
// ConversationDTO.java
public class ConversationDTO {
    private UserDTO partner;          // 对话对象
    private MessageDTO lastMessage;    // 最后一条消息
    private long unreadCount;          // 未读数量
}

// MessageDTO.java
public class MessageDTO {
    private Long id;
    private Long senderId;
    private String content;
    private Boolean isRead;
    private LocalDateTime createdAt;
}
```

---

## 🎯 实现步骤

### Phase 1: 后端实现

1. **创建实体类**
   - `PrivateMessage.java`

2. **创建Repository**
   - `PrivateMessageRepository.java`
   - 自定义查询方法

3. **创建Service**
   - `MessageService.java`
   - 实现业务逻辑

4. **创建Controller**
   - `MessageController.java`
   - 提供RESTful API

5. **创建DTO**
   - `ConversationDTO.java`
   - `MessageDTO.java`
   - `SendMessageRequest.java`

### Phase 2: 前端集成

1. **修改导航栏**
   - 在所有端的主页添加"消息"入口
   - 显示未读数量

2. **创建消息页面**
   - `pages/Common/Messages.vue`
   - 左右分栏布局

3. **创建消息列表组件**
   - `MessageList.vue`
   - 显示对话列表

4. **创建聊天窗口组件**
   - `ChatWindow.vue`
   - 显示历史消息

5. **创建API文件**
   - `api/message.js`
   - 封装API调用

### Phase 3: 数据库更新

1. **更新schema.sql**
   - 添加 `private_message` 表

2. **创建迁移脚本**
   - 供已有数据库使用

---

## 🎨 详细界面效果

### 导航栏入口显示

**学生端：**
```
┌────────────────────────────────────────┐
│ AI五感作文训练平台    💬 消息(5) 🚪退出│
└────────────────────────────────────────┘
```

**教师端：**
```
┌────────────────────────────────────────┐
│ AI五感作文训练平台    💬 消息(2) 🚪退出│
└────────────────────────────────────────┘
```

**家长端：**
```
┌────────────────────────────────────────┐
│ AI五感作文训练平台    💬 消息(1) 🚪退出│
└────────────────────────────────────────┘
```

### 消息页面（左侧列表示例）

```
┌──────────────────────────────────┐
│ 👤 消息列表                      │
├──────────────────────────────────┤
│ ● 📩 张老师                      │
│   今天作文怎么写？                │
│   19:30          2 未读          │
├──────────────────────────────────┤
│    📩 妈妈                       │
│    孩子最近学习怎么样？           │
│    昨天  ✓                        │
├──────────────────────────────────┤
│    📩 李老师                     │
│    你的作文写得很好               │
│    周一   ✓                       │
└──────────────────────────────────┘
```

---

## ✅ 确认要点

1. **消息入口只在主页显示**
   - 每个端的顶部导航栏添加"💬 消息"
   - 显示未读数量角标

2. **点击后才打开聊天界面**
   - 默认首页保持不变
   - 只有点击"消息"才进入私信页面

3. **独立的消息页面**
   - 左侧：对话列表
   - 右侧：聊天窗口（未选中时显示空状态）

4. **不影响现有功能**
   - 学生、教师、家长端的其他功能保持不变

---

## 🚀 预计实现时间

- 后端：2-3小时
- 前端：3-4小时
- 总计：5-7小时

**开始实现吗？** ✨

