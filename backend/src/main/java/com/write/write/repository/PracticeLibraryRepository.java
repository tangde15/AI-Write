package com.write.write.repository;

import com.write.write.entity.PracticeLibrary;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PracticeLibraryRepository extends JpaRepository<PracticeLibrary, Long> {}
